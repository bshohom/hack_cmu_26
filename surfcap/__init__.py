"""Photo-to-metric-surface registration runtime.

Run with ``python -m surfcap <capture-folder> --out <dir>``. The Streamlit
workflow calls this through ``tools/registration.py`` and never imports the
GPU model stack into the UI process.
"""
