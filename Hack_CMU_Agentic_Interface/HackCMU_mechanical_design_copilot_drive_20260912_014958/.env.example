CURSOR_API_KEY=

# Optional runtime overrides. By default, the vendored root surfcap package runs with
# the same Python executable as Streamlit. Set SURFCAP_PYTHON only when keeping the
# heavy registration dependencies in a separate environment.
SURFCAP_ROOT=
SURFCAP_PYTHON=
# live | off
SURFCAP_MODE=live
